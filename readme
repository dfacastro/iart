touch readme

git config --global user.name "Diogo Castro"
git config --global user.email "b.boy.souljah@gmail.com"
git config --global github.user dcastro
git config --global github.token <token>

git remote add origin git@github.com:dcastro/iart.git

//commit
git add .
git commit -am "<comment>"
git push -u origin master

//clone
git clone git://github.com/dcastro/iart.git

//update

cd ~/.ssh
ssh-keygen -t rsa -C "<email>"
//adicionar ssh key no painel de controlo
ssh git@github.com